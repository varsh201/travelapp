#include <stdio.h>

int main() {
    int a = 10;
    float b = 3.14;
    if (a > b) {
        printf("a is greater than b\n", a);
    } else {
        printf("b is greater than or equal to a\n", b);
    }
    return 0;
}